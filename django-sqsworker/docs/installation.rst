============
Installation
============

At the command line::

    $ easy_install django-sqsworker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sqsworker
    $ pip install django-sqsworker
